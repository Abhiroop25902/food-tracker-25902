rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /food-pics/{userId}/{allPaths=**} {
      // Allow users to read and write only their own files
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
